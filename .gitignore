node_modules/
dist/
.wrangler/
*.tsbuildinfo
.DS_Store
# Real presentations used as fixtures are private and stay local. The
# synthetic one is generated by scripts/make-test-deck.mjs and IS committed,
# so the suite tests something on a clean clone and in CI.
test/fixtures/*.pptx
!test/fixtures/synthetic.pptx

# Tauri build output
src-tauri/target/
src-tauri/gen/
