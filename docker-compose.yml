# Generated by stoatworks-unraid/scripts/gen-docker.mjs from fleet.json.
# Repo: pptx-font-manager. Edit fleet.json and regenerate — direct edits are overwritten.
#
# Pull the published image by default. Uncomment build: . to build from this
# checkout instead — useful when testing a change before it reaches GHCR.

services:
  pptx-font-manager:
    image: ghcr.io/stoatworks-labs/pptx-font-manager:latest
    # build: .
    container_name: pptx-font-manager
    restart: unless-stopped
    ports:
      - "8538:80"
